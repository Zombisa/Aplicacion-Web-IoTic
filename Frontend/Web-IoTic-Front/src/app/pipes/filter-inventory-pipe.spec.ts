import { FilterInventoryPipe } from "./filter-inventory-pipe";


describe('FilterInventoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInventoryPipe();
    expect(pipe).toBeTruthy();
  });
});
